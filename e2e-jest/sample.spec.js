describe('basics', () => {

    it('should find an element by text', async () => {
        await new Promise(resolve => setTimeout(resolve, 10000))
        // await this.driver.findElementByAccessibilityId('gotoOtherButton')
    })

})
